# Copy to .env and fill in. Never commit .env.
# Get a key at https://elevenlabs.io/app/settings/api-keys (API Pro/Scale tier for video credits).
XI_API_KEY=
# Optional: default narration voice id (from `node -e "import('./lib/elevenlabs.mjs').then(m=>m.listVoices()).then(v=>console.log(v.voices.map(x=>x.name+' '+x.voice_id)))"`)
XI_VOICE_ID=
