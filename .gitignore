# secrets
.env

# deps
node_modules/

# generated media + cache (regenerate via `npm run mock` or a live run)
.cache/
output/*.mp4
output/*.mp3
output/*.aiff
output/*.srt

# the run manifest IS the case-study evidence: committed deliberately

# os
.DS_Store

# personal working scripts, never public
input/voice-clone-script.md
input/field-teams-script.md

# session handoff docs, never public
HANDOFF*.md
# sound-loop receipts: the review JSONs are evidence, the stems are 400MB of wav
output/sound-loop/**
!output/sound-loop/**/
!output/sound-loop/**/*.json
